import {
  Button,
} from "@chakra-ui/react";


export const JoinGameButton: React.FC = () => {

  return (
      <Button variant="solid" size="xs">
        G
      </Button>
  );
};

export default JoinGameButton;
